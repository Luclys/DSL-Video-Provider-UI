export interface IHeaderAttributes {
  logo?: boolean;
  searchBar?: boolean;
  darkmodeBtn?: boolean;
  userAvatar?: boolean;
  title?: boolean;
  tableOfContent?: boolean;
}

export interface ILink {
  title: string;
  link: string;
}